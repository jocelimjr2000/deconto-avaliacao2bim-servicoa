server:
   port: 20000
   
spring:
   messages:
      encoding: UTF-8

   mvc:
      pathmatch:
         matching-strategy: ant_path_matcher
         
   # RabbitMQ
   rabbitmq:
      host: localhost
      port: 5672
      username: admin
      password: admin
      publisher-confirm-type: correlated
      publisher-returns: true
